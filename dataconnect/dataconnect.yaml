specVersion: "v1"
serviceId: "web site"
location: "europe-central2"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "y"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
